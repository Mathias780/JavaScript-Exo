//------------------------------------------- Input Logging --------------------------------------//

const input = document.getElementById("logMe")

input.addEventListener("input", () => {
    console.log(input.value)
})

// Input.value gives you the current text inside the field

//input event fires each time a character is typed, deleted, pasted, etc.

//display in web site console 

